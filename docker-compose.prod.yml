version: '3'

services:

  etl:
    build:
      target: prod

  orders:
    build:
      target: prod

  users:
    build:
      target: prod

  notifications:
    build:
      target: prod

  generator:
    build:
      target: prod